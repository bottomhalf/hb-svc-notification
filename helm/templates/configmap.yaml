apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "hb-svc-notification.fullname" . }}-config
  namespace: {{ .Values.namespace }}
data:
  {{- range $key, $val := .Values.configData }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}
